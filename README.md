# hexdump

A miniature implementation of the `hexdump` file utility, written in C.

## Usage

`./hexdump <filename>`.

As per the current state of the program, the filename is assumed to be sane, or otherwise, the file is assumed to exist.

## Notes

Additional flags may be implemented to alter the output for readability or usage.
